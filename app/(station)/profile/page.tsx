import ProfileView from "@/app/components/profile/ProfileView";

export const metadata = {
  title: "Operator Profile — BGI Ground Station",
};

export default function ProfilePage() {
  return <ProfileView />;
}
